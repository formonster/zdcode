# 快速开始

tita template 命令可以实现在项目中快速创建模板代码。

## 安装

### 设置私有源

目前 cli 在私有源上，需要修改一下全局的 .npmrc 配置。

```
@zdcode:registry=http://43.143.167.128:4000
```

### 安装 cli

```bash
$ yarn global add @zdcode/cli
```

> 主要这里必须使用 yarn，用 npm 会报错